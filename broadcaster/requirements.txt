mediapipe==0.10.35
opencv-python==4.10.0.84
python-socketio[client]==5.12.0
python-dotenv==1.0.1
numpy==1.26.4
# LiveKit RTC client SDK — used by broadcaster/publisher.py to publish
# composited camera frames as a WebRTC video track. See
# docs/decisions/001-livekit-over-mux.md.
livekit>=1.0.0,<2.0.0
